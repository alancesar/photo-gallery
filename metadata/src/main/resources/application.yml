server:
  port: ${PORT:8081}
  servlet:
    context-path: /api

minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ROOT_USER:photo-gallery}
  secret-key: ${MINIO_ROOT_PASSWORD:photo-gallery}
  bucket:
    name: ${PHOTOS_BUCKET:photos}
